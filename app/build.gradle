/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/8.0.2/userguide/building_java_projects.html
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

compileJava {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}

compileTestJava {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // Use JUnit Jupiter for testing.
    testImplementation 'org.junit.jupiter:junit-jupiter:5.9.1'

    // This dependency is used by the application.
    implementation 'com.google.guava:guava:31.1-jre'
    
    // Hibernate ORM
    implementation 'org.hibernate:hibernate-core:5.6.15.Final'
    
    // JPA API
    implementation 'javax.persistence:javax.persistence-api:2.2'
    
    // PostgreSQL Driver
    implementation 'org.postgresql:postgresql:42.6.0'
    
    // Hibernate Connection Pool (C3P0)
    implementation 'org.hibernate:hibernate-c3p0:5.6.15.Final'
}

application {
    // Define the main class for the application.
    mainClass = 'pap2025.App'
}

// Task para ejecutar CrearTablasIniciales
task crearTablas(type: JavaExec) {
    group = 'application'
    mainClass = 'pap2025.persistencia.CrearTablasIniciales'
    classpath = sourceSets.main.runtimeClasspath
}

// Task para probar autenticación
task testAuth(type: JavaExec) {
    group = 'application'
    mainClass = 'pap2025.TestAutenticacion'
    classpath = sourceSets.main.runtimeClasspath
}

tasks.named('test') {
    // Use JUnit Platform for unit tests.
    useJUnitPlatform()
}
